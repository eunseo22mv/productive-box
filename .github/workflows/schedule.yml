name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ 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 }}
          GIST_ID: ${{ 2161eed7cc8c2b366ff593415fcce6f8 }}
          TIMEZONE: Asia/Seoul
